import React from "react";

const Total = ({ total }) => {
	return (
		<div className='total-container'>
			<span>Total Items Remaining: {total}</span>
		</div>
	);
};
export default Total;
